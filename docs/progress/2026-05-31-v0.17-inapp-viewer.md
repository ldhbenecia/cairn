# 2026-05-31 — v0.17-inapp-viewer

> 진행 단계: **v0.17 — 인앱 노션 뷰어** (목록 행 클릭 시 우측 드로어)
> 상태: 완료 (PR 준비)

## 완료

- **페이지 내용 조회** (`notion-client.ts` fetchPageContent) — workspaceLabel 로 토큰 선택 → `blocks.children.list` → SimpleBlock[] 로 단순화 (heading/paragraph/bulleted·numbered/to_do/quote/callout/code/divider)
- IPC `cairn:notion:page-content` + preload + 타입
- **WorklogDrawer** — 행 클릭 시 우측에서 슬라이드 인(translate-x), 바깥(백드롭)·X 클릭 시 슬라이드 아웃(200ms). 헤더에 제목·날짜·ws + "노션에서 열기"(외부) + 닫기
- 블록 렌더러: 노션 페이지 내용을 앱 안에서 바로 확인 (화면 이동 X)
- worklog-list 행 클릭: 외부 노션 열기 → 드로어 열기로 변경 (외부 열기는 드로어 헤더 버튼으로 유지)
- 실제 worklog 페이지로 검증 (Summary/Done/In Progress + bullet 정상 렌더)

## 시행착오 / 결정

- 토큰 선택: RecentPage.workspaceLabel → config 의 워크스페이스 토큰 매핑 (없으면 첫 ws)
- 블록은 page_size 100, 1단계 깊이만(중첩 toggle 자식 미전개) — v1 충분
- 노션 내용 표시는 egress 아님(노션에서 가져와 로컬 표시). ADR 0003 은 외부로 보내는 것 — 무관

## 다음 (백로그)

- 중첩 블록(toggle 자식), 이미지/임베드
- GitHub device flow OAuth (토큰 붙여넣기 제거)
- worklog.config.example.json 정리(SETUP/README 참조 동반 수정)
