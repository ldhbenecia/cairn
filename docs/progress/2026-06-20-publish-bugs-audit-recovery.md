# 2026-06-20 — publish-bugs-audit-recovery

> 진행 단계: **발행 파이프라인 버그 정리 + 전 기간 백필 + 코드 감사** (마무리)
> 상태: 진행 중 (PR 리뷰 대기)

발행/통계 관련 버그를 연달아 잡고, 2월~오늘 전체를 재발행했으며, 마지막으로 코드/비즈니스 감사를 돌려 추가 버그를 수정했다.

## 완료

### 발행 흐름·통계 (앞선 작업 연속)
- 커밋·PR 카운트 정의 통일(distinct 커밋·전체 PR), 통계 로컬 소스 전환(ADR 0027), 계정별 요약, 발행 취소, 백필 진행 UI(일자 셀·동시 처리·메인 구조화 브로드캐스트), 요약 실패 처리(가짜 성공 방지).

### 2월이 비던 진짜 원인 — GitHub PR 검색 누락 (핵심 버그)
- 증상: "3월 5일부터" 일지가 잡히고 2월은 비어 보임. 사용자는 2월에 회사(Work 계정) 작업을 함.
- 진단: Work 계정에서 **2월 작성 PR 3개**(2/6·2/10·2/24)가 전부 **3월에 머지돼 `updated_at`이 밀림**. cairn 은 `updated:` 기준으로 검색하는데 `searchPrs` 가 **첫 100건(best-match)만** 받아(페이징 없음), 넓은 백필 범위에서 오래된 PR 이 잘렸다. "3/5"가 그 100건 컷 경계.
- 수정: `searchPrs` 를 **updated desc 정렬 + 페이징(최대 1000)**. 검증 — 2/24·2/10·2/6 PR 정상 수집.

### 전 기간 백필 (2026-02-03 ~ 오늘, 전부 force)
- 일간: CLI 백필 캡을 일시 366 으로 올려 한 번에 140일 force(동시성 4) → 발행 76일. 실패 2일(05-06·06-10, Claude 일시 실패)은 재발행. 이후 searchPrs 수정 반영해 2월 구간 재발행 → 로컬 통계 76→90일.
- 주간(20)·월간(5) 롤업 force. (1차 루프는 zsh 가 `for d in $WEEKLY` 공백 분리를 안 해 주간 전부 실패 → `while IFS= read -r` 로 재실행, 20/20 성공.)
- CLI 캡은 60 으로 복귀(긴 범위는 추후 유료 UI).

### 코드/비즈니스 감사 → 수정
4개 영역 병렬 서브에이전트 감사. 검증된 실버그만 수정:
- **[HIGH] force 발행 데이터 손실 창**: archive 먼저 → create 실패 시 일지 소실. → create 후 archive(손실 없음). `findWorklogPageByDate` 에 created_time desc 정렬(중복 시 최신 선택).
- **[MED] operator raw-dump egress 우회**: CairnError 메시지의 절대경로가 노션에 샐 수 있음. → `assertNoForbiddenPayload` fail-closed redact.
- **[MED] 에러 경로 알림 누락**: spawn 실패 시 완료 알림 안 옴. → error 핸들러에서도 알림.
- **[MED] 스냅샷 복원 레이스**: 마운트 중 라이브 세션을 덮어씀. → 기존 세션 있으면 bail.
- **[LOW] tray busy 거부 uncaught**: → catch 추가.

## 시행착오 / 결정
- **감사 신뢰성**: 이전에 "버그 없음"이라 했다가 계속 나온 전례 → 이번엔 영역 분할 + 추측 금지 + file:line·시나리오·심각도 강제. CLAUDE.md 에 "검증 전 단언 금지(#5)" 원칙 추가.
- **보류한 감사 항목(근거)**: CLI↔앱 동시실행 락(개발자 전용 엣지), 레거시 폴백(ADR 0027 + 재발행으로 해소), shortSha 교차 repo 충돌(1/16⁷·트레이드오프), DST streak(한국 무관), 윈도우 sub-second tail(이론적).

## 다음
- 누적 변경 main 머지 + 버전 bump.
- 회사 백엔드는 이 노트북 로컬 클론이 없어 Work GitHub 로만 수집(페이징 수정으로 과거분 포함).
