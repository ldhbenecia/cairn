# 2026-05-31 — v0.17-onboarding (PR-2)

> 진행 단계: **v0.17 PR-2 — 온보딩/셋업 위저드** (진입장벽 해소)
> 상태: 완료 (PR 준비)
> plan: [docs/plans/2026-05-31-v0.17-onboarding-detail.md](../plans/2026-05-31-v0.17-onboarding-detail.md)

## 완료

- **셋업 완료 판정** (`main/setup.ts` isSetupComplete) — worklog.pageId 있는 notionWorkspace + 토큰 존재. bootstrap-sync 에 `setupComplete` 추가 → 미완 시 App 이 온보딩 표시(무플래시)
- **온보딩 main IPC** (`main/onboarding.ts`):
  - `probeNotion` — users.list → person 목록(=myUserId 후보)
  - `searchNotion` — search(page) → 발행 부모 페이지 목록
  - `probeGithub` — GET /user → login
  - `finish` — `.env`(라인 보존 병합) + `worklog.config.json` 작성
  - `pickFolder` — 로컬 repo 폴더 선택
- **위저드 UI** (`onboarding.tsx`): Welcome → Notion → GitHub → Claude → Repos → 완료
  - **멀티 계정**(노션/깃허브 추가·삭제), 라벨 기반 env 키
  - 노션: 토큰 테스트 → person 선택(복수 시) → **페이지 검색**해서 부모 선택
  - GitHub: 토큰 테스트(login 표시)
  - Claude: CLI 자동 인계 안내 + (선택) API key
  - 라이브 테스트(노션·깃허브), 실제 토큰으로 API 모양 검증 완료(person id == 기존 myUserId 일치)
- **Preferences "셋업 다시 실행"** + 재실행 시 취소(이미 셋업된 경우 갇히지 않게)

## 시행착오 / 결정

- pageId 만 받으면 됨 — worklog/rollup DB 는 첫 발행 시 자동 생성
- Claude 인증 토큰 강제 X (ADR 0009 멀티오토: CLI 인계 자동)
- 노션 부모 페이지: 검색 API 목록 선택 / 멀티 계정 / 라이브 테스트 (확정)

## 알려진 후속 (follow-up)

- **온보딩 UI 문자열 i18n 미적용**(현재 ko 하드코딩) — 첫 실행 en 사용자용으로 후속 추출
- 노션 검색은 page 50개까지(페이지네이션 없음) — 부모가 안 보이면 추후 검색어 입력 추가
- Claude 실제 연결 테스트(요약 probe)는 미구현(quota 고려, 안내만)

## 다음

- PR-3 커스텀 프롬프트 (settings.prompts override)
