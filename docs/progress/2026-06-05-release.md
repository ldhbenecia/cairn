# 2026-06-05 — release

> 진행 단계: **배포 + 자동 업데이트** (진행 중)
> 플랜: [2026-06-04-release-auto-update.md](../plans/2026-06-04-release-auto-update.md) · ADR [0017](../decisions/0017-anonymous-telemetry.md)

## 완료
### 단계 1 — electron-builder dist 설정
- mac target `dir` → **dmg + zip**(arm64). zip 은 electron-updater 용
- `publish: github`(ldhbenecia/cairn), `mac.identity: null`(미서명), `author` 추가
- 로컬 `pnpm dist` 검증 — `cairn-<ver>-arm64.dmg` + `.zip` + `latest-mac.yml` + blockmap 생성 확인. 코드 서명 스킵됨
- artifact 버전은 desktop 모듈 버전(로컬). 릴리스 워크플로에서 **root 버전 주입**(`--config.extraMetadata.version`) 예정 → 앱 표시·태그·updater 모두 root 정렬

## 진행 중 / 다음
- 단계 2 — electron-updater check+notify (미서명이라 자동 적용 X)
- 단계 3 — release.yml(tag v* → 빌드+publish, root 버전 주입·검증) + ci.yml
- 단계 4 — README 배포 섹션(미서명 안내)
- 단계 7 — 첫 릴리스: public 전환 + `git tag v0.17.x` push

## 메모
- private 동안 Notion Claude 아이콘·About Star 안 뜸 → public 전환 시 복구(코드 변경 불필요)
