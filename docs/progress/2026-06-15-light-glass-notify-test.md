# 2026-06-15 — light-glass-notify-test

> 진행 단계: 라이트 테마 보정 · 발행 알림 권한 UX (사용자 피드백)
> 상태: 완료 (라이트 글래스는 사용자 화면 검증 필요)

## 완료
- **라이트 모드 글래스 보정** — `.glass-panel` 기본값이 다크 기준이라 라이트에서 탁하게 떴음. `[data-theme='light'][data-glass='on']` 전용 override: 불투명도 74~84%, sheen 위주 inset, 어두운 그림자 제거, overlay 연하게.
- **발행 알림 권한 테스트 버튼** — 알림 탭에 "테스트 알림 보내기". `sendTestNotification`(설정 토글 무관 항상 시도) → macOS 권한 프롬프트 유도 + 표시 확인. 설명에 "안 뜨면 시스템 설정 → 알림에서 cairn 허용" 안내.

## 시행착오 / 결정
- 발행 완료 알림 코드(`sendResultNotification`)는 정상 — 안 뜨는 건 macOS 알림 **권한** 문제로 추정. 테스트 버튼으로 사용자가 권한을 직접 확인·허용하게 함.
- **Electron 렌더러는 미리보기 검증 불가** → 라이트 글래스 색은 사용자 화면에서 확인하고 값 조정 필요(memory: 색은 렌더 화면에서 판단).

## 다음
- 사용자 라이트 모드 확인 후 글래스/색상 미세 조정 (피드백 받아 iterate)
