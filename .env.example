NODE_ENV=development

# 토큰은 온보딩이 계정 label 별로 자동 기입 (GITHUB_TOKEN_<LABEL> / NOTION_TOKEN_<LABEL>)
GITHUB_TOKEN_PERSONAL=
NOTION_TOKEN_PERSONAL=

# CAIRN_HOME (셸 env 전용): 패키지 앱 데이터 디렉토리(~/.cairn) override. 이 .env 안에선 무효
