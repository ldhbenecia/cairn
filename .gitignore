# dependencies
node_modules/

# build
dist/
*.tsbuildinfo

# secrets
.env
.env.*
!.env.example

# machine-local config (absolute repo paths inside, never commit)
worklog.config.json

# runtime data (cairn local state lives at ~/.cairn/, but never commit if mirrored)
.cairn/

# logs
*.log
logs/

# coverage / test artifacts
coverage/

# editor / OS
.DS_Store
Thumbs.db
.eslintcache

# Claude Code per-machine local settings (project .claude/settings.json is committed)
.claude/settings.local.json

# 내부 문서 — 로컬에서만 관리(공개 OSS 레포에 ADR/계획/진행/노트 노출 안 함).
# 공개 유지: docs/SETUP*.md, docs/assets/
docs/decisions/
docs/plans/
docs/progress/
docs/notes/
